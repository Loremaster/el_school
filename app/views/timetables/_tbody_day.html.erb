<tbody>
  <% timetables_for_day.each do |t| %>
    <tr class="cross" onclick = "location.href='<%= edit_timetable_path( :id => t.id ) %>'">
      <%= render :partial => 'tds', :locals => { :t => t } %>
    </tr>
  <% end %>
</tbody>